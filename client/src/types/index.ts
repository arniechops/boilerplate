// ---------------------------------------------------------------------------
// Shared TypeScript types
// ---------------------------------------------------------------------------
// Place domain-level types here when they are used across multiple features.
// Feature-specific types can live alongside their hook / component instead.

export interface ApiError {
  message: string;
}
