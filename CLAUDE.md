# Boilerplate — Agent Guide

Stack and setup details live in the root `README.md`. Layer-specific conventions live in `api/CLAUDE.md` and `client/CLAUDE.md`.

## MCP usage policy

- **Context7** — before writing code against an unfamiliar library, framework, or a version newer than your training data, resolve it via Context7 and pull current docs first. Don't rely on memorized API shapes for fast-moving dependencies.
